const {computeTransitionsAndStatesFromXmlString,} = require ('./yed2kingly');

// TODO: latest version of kingly does not seem to export properly historyState fn?
// TODO: add the conversion to plantUML here too

module.exports = {
  computeTransitionsAndStatesFromXmlString
};
